# Go 构建产物
dist/

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp
.cursor/

# local runtime state (manifest 在 git 里跨机同步; 临时态不入 git)
*.local-state.json
*.log
# 本机身份绑定(machines.toml 里的机器别名)— 每台机器各自绑定, 不跨机
.skillbank-machine
# skill 分发包内的二进制(make skill 从 dist/ 拷入, 不入库)
skill/bin/