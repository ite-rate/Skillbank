[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "skillhub"
version = "0.1.0"
description = "Central skill repository syncing canonical SKILL.md to 7 AI agents via IR (parser->IR->emitter). body byte-identical, no loss."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "ss", email = "deepl@example.com" }]
keywords = ["skill", "agent", "sync", "central-repo", "anthropic-skill"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "PyYAML>=6.0",       # SKILL.md frontmatter 是 YAML,parser/emitter 必需
    "tomli_w>=1.0",      # importer 写 .agent_overrides/*.toml(读用内置 tomllib)
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",       # 跑零损耗往返测试
    "pytest-cov>=5.0",
]

[project.scripts]
skillhub = "skillhub.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
skillhub = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra --strict-markers"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]